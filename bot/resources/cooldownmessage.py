# Create the message embed
#
# preface_message = "**You've messaged me way too many times!** I'll show the message to __only you__, but no one else. It's our little secret!*\n"
# embed = discord.Embed(color=discord.Color.blue())
# embed.add_field(name='', value=preface_message, inline=False)
# embed.add_field(name='CBS Bot Message', value=message, inline=False)
# embed.set_footer(text="Note: This message is sent ephemerally. It is only shown to you.")
# await ctx.send(embed=embed, ephemeral=True, silent=True)